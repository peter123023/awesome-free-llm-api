# 参与贡献 Awesome Free LLM API

感谢帮助维护这份列表的准确性。核实、修正、新增条目的贡献同样宝贵。

英文版说明：本文档为中文版，如需英文阅读请配合 [README.en.md](README.en.md)。

## 收录门槛

本列表**只收录两类**，判断标准：**能申请 API Key、通过 endpoint 程序化调用、能持续免费使用**。

- **永久免费**（`permanent-free`）— 按天/月重置的免费层，或官方指定的永久免费模型；
- **限时免费**（`limited-free`）— 平台明确标注「限时免费 / Limited-Time Free」的模型或活动。

仅网页对话免费（官网 Chat 界面）不算；注册赠送额度不算。

## 渠道条目模板

新增渠道用以下结构，所有字段必填，不确定写 `未知`：

```markdown
### [渠道名]

- **官网**：https://官方地址（API 文档如有，一并列出）
- **免费形式**：永久免费层 / 永久免费模型 / 限时免费，写具体额度（次/天、token/月、RPM）
- **网页说明**：官网对免费服务的原文要点（引号包裹）
- **免费模型**：当前免费提供的模型清单
- **接入**：OpenAI 兼容 Base URL / 原生 SDK / 见官方文档
- **状态**：**Active** / **Active（限时）** — 核实于 `YYYY-MM-DD`
```

状态只有三种：**Active**（当前可用）、**Active（限时）**（限时活动，随时结束）、**Archived**（失去免费资格，标日期说明变化，不要删除）。

## 模型索引同步

渠道条目要与[模型免费渠道索引](README.md#模型免费渠道索引)一一对应。索引格式：

- 每个模型一张表，一行一个渠道；
- 第一列渠道名+官网链接，第二列免费形式，第三列**优势/限制**（额度、是否需要实名/绑卡、是否限时等）；
- 排序：国产模型在前，免费额度高且稳定的在前；免费质量低（额度小/共享限流）的渠道放表格末尾。

## 规则

1. **先核实再添加。** 以官方定价/限速页为准，博客、聚合站只当线索，PR 附链接。
2. **变动不删除。** 渠道失去免费资格标 **Archived**（附日期），说明变了什么。
3. **只用官方链接。** 推广/返利链接会被移除。
4. **标注免费分类。** 拿不准时标 `permanent-free` / `limited-free`。

## 本地预览

纯 Markdown，无需构建：

```bash
npx markdownlint-cli2 README.md README.en.md CONTRIBUTING.md
```

## 许可证

提交贡献即表示你同意内容以与本仓库相同的 [MIT](LICENSE) 许可证发布。
